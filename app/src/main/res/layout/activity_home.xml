<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.enstax.cesarcano.hellogas.ui.view.home.HomeActivity">

    <include layout="@layout/content_home" />
    <android.support.design.widget.BottomNavigationView
        android:id="@+id/navigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_navigation_view"
        android:background="@android:color/white"
        android:layout_gravity="bottom"
        android:theme="@style/Widget.Compat.NotificationActionText"
        app:menu="@menu/activity_home_navigation"/>
</android.support.design.widget.CoordinatorLayout>